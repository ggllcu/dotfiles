session: tool-reseller-api

root: ~/works/tool-reseller-api/

windows:
  - name: code 
    commands: 
    - nvim 
  - name: run
    manual: true
    commands:
    - mix phx.server
  - name: test
    commands:
    - docker-compose up -d
  - name: db
    manual: true
    commands:
    - docker-compose up -d
  - name: git
    commands: 
    - git status 
